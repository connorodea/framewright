# framewright — GitHub Pages source

This directory hosts the static landing site at https://connorodea.github.io/framewright/.

To deploy: Settings → Pages → Deploy from a branch → `main` → `/docs`.

Single file: `index.html` (inline CSS + JS, no build step). `.nojekyll` disables Jekyll.
